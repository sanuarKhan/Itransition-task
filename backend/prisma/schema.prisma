generator client {
  provider = "prisma-client-js"
  

}

datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  pass      String
  name      String
  img       String?
  lang      Language @default(EN)
  theme     Theme    @default(LIGHT)
  role      Role     @default(USER)
  isBlocked Boolean  @default(false)
  templates Template[]
  forms     Form[]
  comments  Comment[]
  likes     Like[]
  accessTo  TemplateAccess[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Template {
  id            String     @id  @default(uuid())
  title         String
  description   String  //TODO: markdown support
  topic         Topic
  thumbnail     String?
  isPublic      Boolean         @default(true)
  ownderId      String
  owner         User      @relation(fields: [ownderId], references: [id], onDelete: Cascade)
  allowedUsers  TemplateAccess[]
  questions     Question[]
  forms         Form[]
  comments      Comment[]
  likes         Like[]
  tags          TagOnTemplate[]
}

model TemplateAccess {
  id            String     @id  @default(uuid())
  templateId    String
  userId        String
  template      Template   @relation(fields: [templateId], references: [id], onDelete: Cascade)
  user          User       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([templateId, userId])
}

model Tag {
  id            String    @id   @default(uuid())
  name          String    @unique
  templates     TagOnTemplate[]
}

model TagOnTemplate {
  templateId    String
  tagId         String
  templete      Template   @relation(fields: [templateId], references: [id], onDelete: Cascade)
  tag           Tag        @relation(fields: [tagId], references: [id], onDelete: Cascade)

  @@id([templateId, tagId])
}

model Question {
  id            String    @id  @default(uuid())
  templateId    String
  title         String
  description   String?
  type          QuestionType
  showInTable   Boolean   @default(false)
  order         Int
  isRequired    Boolean   @default(false)
  template      Template  @relation(fields: [templateId], references: [id], onDelete: Cascade)
  answers       Answer[]
}

model Answer {
  id            String   @id  @default(uuid())
  questionId    String
  formId        String
  valueText     String?
  valueInt      Int?
  valueBool     Boolean?
  question      Question @relation(fields: [questionId], references: [id], onDelete: Cascade)
  form          Form     @relation(fields: [formId], references: [id], onDelete: Cascade)
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}

model Form {
  id            String   @id  @default(uuid())
  templateId    String
  userId        String
  template      Template @relation(fields: [templateId], references: [id], onDelete: Cascade)
  user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  answers       Answer[]

  @@unique([templateId, userId])
}

model Comment {
  id          String  @id  @default(uuid())
  templateId  String
  userId      String
  content     String
  template    Template  @relation(fields: [templateId], references: [id], onDelete: Cascade)
  user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
}

model Like {
  id          String @id @default(uuid())
  templateId  String
  userId      String
  template    Template  @relation(fields: [templateId], references: [id], onDelete: Cascade)
  user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@unique([templateId, userId])

}

enum Role {
  USER
  ADMIN
}

enum Language {
  EN
  RU
  BN
}

enum Theme {
  LIGHT
  DARK
}

enum Topic {
  EDUCATION
  BUDGETS
  QUIZZ
  REPORTS
  RESEARCH
  SURVEY
  PULL
  OTHERS
}

enum QuestionType {
  SINGLE_LINE
  MULTI_LINE
  INTEGER
  CHECKBOX
}

